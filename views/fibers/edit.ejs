<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head.ejs") %>

    <body>
        <%- include("../partials/header.ejs") %>
            <main>
                <div>
                    <form action="/fibers/<%= fiber._id %>?_method=PUT" method="post">
                        <fieldset>
                            <legend>Edit a <%= fiber.name %>
                            </legend>
                            <label>
                                NAME:<input type="text" name="name" value="<%= fiber.name %>" placeholder="enter fiber name" />
                            </label>
                            <label>
                                COLOR:<input type="text" name="color" value="<%= fiber.color %>" placeholder="enter fiber name" />
                            </label>
                            <label>
                                READY TO EAT:<input type="checkbox" name="readyToEat" <%=fiber.readyToEat ? "checked" : "" %> />
                            </label>
                        </fieldset>
                        <input type="submit" value="Update <%= fiber.name %>" />
                    </form>
                </div>
            </main>
    </body>

</html>