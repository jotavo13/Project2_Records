<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head.ejs") %>

    <body>
        <%- include("../partials/header.ejs") %>
            <main>
                <div>

                    <article>
                        <h2>
                            <%= fiber.name %> - <%= fiber.isItWorking ? "Working" : "Not Working" %>
                        </h2>
                        <h3>
                            <%= fiber.color %>
                        </h3>
                        <a href="/fibers/<%= fiber._id %>/edit"><button>Edit</button></a>
                        <form action="/fibers/<%= fiber._id %>?_method=DELETE" method="POST">
                            <input type="submit" value="Delete" />
                        </form>
                        <a href="/fibers/"><button>Back to Main</button></a>
                    </article>

                </div>
            </main>
    </body>

</html>